import re


n, m = map(int, input().split())

matrix = []
for _ in range(n):
    matrix.append(input())

decoded_string = ""
for column in zip(*matrix):
    decoded_string += "".join(column)

pattern = r'(?<=\w)[^\w]+(?=\w)'
final_output = re.sub(pattern, ' ', decoded_string)

print(final_output)

