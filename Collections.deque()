from collections import deque

d = deque()
n = int(input())

for _ in range(n):
    line = input().split()
    command = line[0]
    
  
    if len(line) > 1:
        value = line[1]
       
        getattr(d, command)(value)
    else:

        getattr(d, command)()

print(*d)
