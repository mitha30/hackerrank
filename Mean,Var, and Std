import numpy


n, m = map(int, input().split())


array = numpy.array([input().split() for _ in range(n)], float)


print(numpy.mean(array, axis=1))


result_std = numpy.std(array, axis=None)
print(round(result_std, 11) if result_std != 0 else result_std)
