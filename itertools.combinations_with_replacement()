from itertools import combinations_with_replacement


s, k = input().split()
k = int(k)


target_string = sorted(s)


result = combinations_with_replacement(target_string, k)


for combo in result:
    print("".join(combo))
